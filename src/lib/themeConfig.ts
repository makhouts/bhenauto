/**
 * Theme Configuration
 *
 * Toggle between light (white) and dark theme for the entire site.
 *
 *   true  = Dark theme  (dark backgrounds, light text)
 *   false = Light theme  (white backgrounds, dark text) — current default
 *
 * Change this flag and restart the dev server to switch themes.
 */
export const DARK_THEME = false;
